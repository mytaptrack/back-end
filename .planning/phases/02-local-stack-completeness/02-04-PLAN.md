---
phase: 02-local-stack-completeness
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [CFG-04, GQL-03]

must_haves:
  truths:
    - "docker compose up starts all services with no container exiting with code 1"
    - "curl to localhost:4000/graphql with a __typename query returns HTTP 200 with JSON"
    - "Subscription query to localhost:4000/graphql returns HTTP 200 (not 500), data is null or structured message"
    - "No manual steps are required beyond docker compose up"
  artifacts: []
  key_links:
    - from: "docker-compose.yml graphql-api service"
      to: "api/package.json container:start"
      via: "Dockerfile CMD entrypoint.sh npm run container:start"
      pattern: "container:start"
---

<objective>
Human verification gate: confirm the Docker stack starts cleanly and that subscription fields return graceful responses.

Purpose: CFG-04 and GQL-03 have manual verification components that cannot be automated — Docker startup state and HTTP subscription behavior require a running stack. This checkpoint captures human sign-off before Phase 2 is marked complete.
Output: Human approval that docker compose up succeeds and all Phase 2 success criteria are visually confirmed.
</objective>

<execution_context>
@/Users/nikody/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikody/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-local-stack-completeness/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 2 gate: verify docker compose up and subscription graceful skip</name>
  <action>
    Human verification required. Plans 02-01 through 02-03 delivered:
    - resolver-wrapper.spec.ts: 5 unit tests for AppSync context shape (all green)
    - api/package.json: container:start now invokes graphql-server.ts (not missing server.ts)
    - resolver-wrapper.ts: identity.claims added to AppSync context
    - graphql-server.ts: null-returning stubs for onUserLicenseChange, onStudentDataChange, studentDataChange

    Follow the how-to-verify steps below to confirm Phase 2 is complete.
  </action>
  <what-built>
    Plans 02-01 through 02-03 delivered:
    - resolver-wrapper.spec.ts: 5 unit tests for AppSync context shape (all green)
    - api/package.json: container:start now invokes graphql-server.ts (not missing server.ts)
    - resolver-wrapper.ts: identity.claims added to AppSync context
    - graphql-server.ts: null-returning stubs for onUserLicenseChange, onStudentDataChange, studentDataChange
  </what-built>
  <how-to-verify>
    Prerequisites — run these first (Claude cannot run docker in this context):
    1. Ensure lib/ and types/ are built:
       ```
       cd /Users/nikody/src/mytaptrack/back-end
       cd lib && npm run build && cd ../types && npm run build && cd ..
       ```
       Required because docker-compose mounts ./lib and ./types as volumes, and @mytaptrack/lib resolves to dist/index.js.

    Step 1 — Start the stack:
    ```
    cd /Users/nikody/src/mytaptrack/back-end
    docker compose up -d
    ```

    Step 2 — Wait ~15 seconds, then verify all containers are running:
    ```
    docker compose ps
    ```
    Expected: all 6 services (dynamodb-local, rabbitmq, redis, graphql-api, device-api, rest-api) show State=running. graphql-api must NOT show "Exit 1".

    Step 3 — Verify GraphQL endpoint responds:
    ```
    curl -s -X POST http://localhost:4000/graphql \
      -H "Content-Type: application/json" \
      -d '{"query":"{ __typename }"}' | python3 -m json.tool
    ```
    Expected: {"data": {"__typename": "Query"}}

    Step 4 — Verify subscription graceful skip (GQL-03):
    ```
    curl -s -X POST http://localhost:4000/graphql \
      -H "Content-Type: application/json" \
      -d '{"query":"subscription { onUserLicenseChange { license } }"}' | python3 -m json.tool
    ```
    Expected: HTTP 200, JSON response. data.onUserLicenseChange may be null. Must NOT be a 500 error or "field not defined by server" GraphQL error.

    Step 5 — Stop the stack:
    ```
    docker compose down
    ```
  </how-to-verify>
  <verify>
    <automated>MISSING — Docker compose verification requires a running Docker daemon. Human must execute steps above.</automated>
  </verify>
  <done>
    Human types "approved" after all 5 steps pass:
    - docker compose ps shows all 6 containers running (no Exit 1)
    - __typename query returns {"data": {"__typename": "Query"}}
    - subscription query returns HTTP 200 JSON (not 500, not "field not defined")
  </done>
  <resume-signal>Type "approved" if all steps passed. Or describe what failed so gaps can be diagnosed.</resume-signal>
</task>

</tasks>

<verification>
Human sign-off via resume-signal "approved" above.
</verification>

<success_criteria>
- docker compose up: all 6 containers reach running state with no exit-code-1 failures
- GET/POST http://localhost:4000/graphql: returns HTTP 200 JSON with __typename: Query
- Subscription query: returns HTTP 200 JSON (null data acceptable, 500 not acceptable)
- Phase 2 complete — ready to plan Phase 3 (Test Validation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-stack-completeness/02-04-SUMMARY.md`
</output>
