---
phase: 02-local-stack-completeness
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - api/package.json
  - api/src/container/resolver-wrapper.ts
autonomous: true
requirements: [CFG-04, GQL-04]

must_haves:
  truths:
    - "npm run container:start in api/ invokes graphql-server.ts, not the non-existent server.ts"
    - "The graphql-api Docker container starts without exiting with code 1"
    - "identity.claims is populated in the AppSync context passed to Lambda handlers"
    - "graphQLWrapper license extraction works: context.identity.groups filter returns license entries"
  artifacts:
    - path: "api/package.json"
      provides: "container:start script pointing to graphql-server.ts"
      contains: "graphql-server.ts"
    - path: "api/src/container/resolver-wrapper.ts"
      provides: "wrapResolver with identity.claims set"
      contains: "claims"
  key_links:
    - from: "api/src/container/entrypoint.sh"
      to: "api/package.json container:start"
      via: "npm run container:start"
      pattern: "container:start"
    - from: "api/package.json container:start"
      to: "api/src/container/graphql-server.ts"
      via: "npx ts-node --transpile-only"
      pattern: "graphql-server.ts"
    - from: "api/src/container/resolver-wrapper.ts wrapResolver"
      to: "identity.claims"
      via: "identity object spread"
      pattern: "claims"
---

<objective>
Fix the two concrete bugs blocking CFG-04 and GQL-04:
1. `container:start` npm script points to non-existent `server.ts` — update to `graphql-server.ts`
2. `resolver-wrapper.ts` does not set `identity.claims` — add `claims: identity` to the identity object

Purpose: The graphql-api container currently exits immediately with code 1. After this plan, the container starts, and the AppSync context shape satisfies the graphQLWrapper contract.
Output: Two file changes. The resolver-wrapper.spec.ts Test 3 (identity.claims) turns GREEN.
</objective>

<execution_context>
@/Users/nikody/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikody/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-local-stack-completeness/02-RESEARCH.md
@.planning/phases/02-local-stack-completeness/02-01-SUMMARY.md

<interfaces>
<!-- Extracted from source files the executor will modify -->

From api/package.json (relevant scripts section — current broken state):
```json
"container:start": "npx ts-node --transpile-only src/container/server.ts",
"graphql:start": "npx ts-node --transpile-only src/container/graphql-server.ts",
```
Fix: change container:start to point to graphql-server.ts.

From api/src/container/resolver-wrapper.ts (current identity line):
```typescript
identity: context.identity || null,  // ← missing claims field
```
Fix: spread identity and add claims field. The identity object from Redis (CognitoIdentity) is the raw JWT payload. Set claims to the identity object itself so apiWrapperEx-style access works.
New shape:
```typescript
identity: context.identity
  ? { ...context.identity, claims: context.identity }
  : null,
```
This preserves all existing fields (groups, username, sub, etc.) and adds claims pointing to the same object. graphQLWrapper reads context.identity.groups (still present). apiWrapperEx reads context.identity.claims.sub (now works).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix container:start script in api/package.json</name>
  <files>api/package.json</files>
  <behavior>
    - After fix: `npm run container:start` runs `npx ts-node --transpile-only src/container/graphql-server.ts`
    - Before fix: `npm run container:start` runs `npx ts-node --transpile-only src/container/server.ts` (file does not exist)
  </behavior>
  <action>
    Read api/package.json. In the "scripts" section, change the value of "container:start" from:
      "npx ts-node --transpile-only src/container/server.ts"
    to:
      "npx ts-node --transpile-only src/container/graphql-server.ts"

    No other changes to package.json. Do not change any other scripts. Do not add or remove dependencies.

    Using graphql-server.ts directly (not server.ts as a shim) per research recommendation — one-line change, zero risk of introducing another indirection layer.

    Verify the file exists before confirming: api/src/container/graphql-server.ts must exist (it does — confirmed in research).
  </action>
  <verify>
    <automated>cd /Users/nikody/src/mytaptrack/back-end/api && node -e "const p = require('./package.json'); const s = p.scripts['container:start']; if (!s.includes('graphql-server.ts')) { process.exit(1); } else { console.log('OK: container:start =', s); }"</automated>
  </verify>
  <done>api/package.json container:start script contains "graphql-server.ts" and NOT "server.ts"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add identity.claims to resolver-wrapper.ts wrapResolver</name>
  <files>api/src/container/resolver-wrapper.ts</files>
  <behavior>
    - After fix: handler is called with appsyncContext.identity.claims set to the raw identity object
    - After fix: appsyncContext.identity.groups and .username still present (not broken by the change)
    - After fix: Test 3 in resolver-wrapper.spec.ts turns GREEN
    - After fix: wrapResolver still returns null identity when context.identity is falsy
  </behavior>
  <action>
    Read api/src/container/resolver-wrapper.ts. Find the identity field in the appsyncContext object:
    ```typescript
    identity: context.identity || null,
    ```

    Replace it with:
    ```typescript
    identity: context.identity
      ? { ...context.identity, claims: context.identity }
      : null,
    ```

    This spreads all existing CognitoIdentity fields (groups, username, sub, email, cognito:groups, etc.) onto the identity object and adds `claims` pointing to the same identity object. This satisfies:
    - graphQLWrapper: reads identity.groups — still present from spread
    - graphQLWrapper: reads identity.username — still present from spread
    - apiWrapperEx: reads identity.claims.sub — now works via claims.sub

    IMPORTANT: Do not modify handler signatures, exports, or any other part of the file. Only the identity line changes.
    IMPORTANT: Do not modify graphql-server.ts wrapResolver (it is a duplicate that will be cleaned up in Phase 3). Only modify api/src/container/resolver-wrapper.ts.
  </action>
  <verify>
    <automated>cd /Users/nikody/src/mytaptrack/back-end/api && npx jest --testPathPattern="resolver-wrapper.spec" --no-coverage --forceExit 2>&1 | tail -20</automated>
    Expected: all 5 tests pass (including Test 3 identity.claims which was RED in Plan 01).
  </verify>
  <done>All 5 tests in resolver-wrapper.spec.ts pass GREEN. identity field in resolver-wrapper.ts uses spread+claims pattern.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `node -e "const p = require('./api/package.json'); console.log(p.scripts['container:start'])"` outputs a string containing "graphql-server.ts"
2. `cd api && npx jest --testPathPattern="resolver-wrapper.spec" --no-coverage --forceExit` — all 5 tests pass
3. `grep -n "claims" api/src/container/resolver-wrapper.ts` shows the claims line present
</verification>

<success_criteria>
- api/package.json: container:start = "npx ts-node --transpile-only src/container/graphql-server.ts"
- api/src/container/resolver-wrapper.ts: identity field includes claims spread
- All 5 resolver-wrapper.spec.ts tests pass (Plan 01 Test 3 goes GREEN)
- No other files modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-stack-completeness/02-02-SUMMARY.md`
</output>
