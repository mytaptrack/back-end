---
phase: 01-config-detection
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - AWS-02
  - CFG-01
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "All unit tests covering USE_LOCAL routing pass green without a running Docker stack"
    - "Handler export signatures in api/src are unchanged from the baseline (AWS-02 satisfied)"
    - "Phase 1 success criteria are verifiably met: USE_LOCAL routes DynamoDB, EventBridge, Cognito, and test config correctly"
  artifacts:
    - path: "system-tests/src/tests/config.spec.ts"
      provides: "All USE_LOCAL routing assertions passing"
      exports: []
    - path: "lib/src/v2/dals/user-dal.spec.ts"
      provides: "USE_LOCAL routing describe block passing"
      exports: []
  key_links:
    - from: "system-tests/src/tests/config.spec.ts"
      to: "system-tests/src/config.ts"
      via: "all endpoint getters return correct values for both modes"
      pattern: "getQLEndpoint|getApiEndpoint|getDeviceEndpoint|getClientId"
---

<objective>
Verify the full Phase 1 success criteria are met: run the unit test suite and perform the static
handler-signature audit required for AWS-02. Human confirms the output before Phase 1 closes.

Purpose: Phase gate. Plans 01 and 02 make the changes; this plan proves the goal is achieved
and that no Lambda handler contracts were broken.
Output: Green unit test run + human-confirmed handler audit. No code changes.
</objective>

<execution_context>
@/Users/nikody/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikody/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-config-detection/01-01-SUMMARY.md
@.planning/phases/01-config-detection/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full unit test suite and capture results</name>
  <files></files>
  <action>
    Run the following commands and capture their output:

    1. lib DAL unit tests:
       cd /Users/nikody/src/mytaptrack/back-end/lib
       npm test -- --testPathPattern="dal.spec|event-dal.spec|user-dal.spec" --passWithNoTests --forceExit

    2. system-tests config and httpClient unit tests:
       cd /Users/nikody/src/mytaptrack/back-end/system-tests
       USE_LOCAL=true npm run test:local -- --testPathPattern="config.spec|httpClient.spec"

    3. Handler signature baseline audit (static — no AWS needed):
       grep -rn "export.*handleEvent\|export function handler\|export const handler\|exports\.handler" \
         /Users/nikody/src/mytaptrack/back-end/api/src \
         --include="*.ts" | grep -v ".spec.ts" | grep -v ".bak"

    Print all output. Do not suppress errors. If any test is red, report the failing test name
    and error message — do not attempt to fix in this plan (open a gap closure instead).
  </action>
  <verify>
    <automated>cd /Users/nikody/src/mytaptrack/back-end/lib && npm test -- --testPathPattern="dal.spec|event-dal.spec|user-dal.spec" --passWithNoTests --forceExit 2>&1 | grep -E "Tests:|PASS|FAIL|passed|failed"</automated>
  </verify>
  <done>
    All three commands have been run and their full output is visible in the executor's response.
    The handler audit grep output shows a list of export lines — none of which were modified by
    Plans 01 or 02 (all changes were in dal.ts constructors, jest.setup.ts, and .env.example).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1 routing hardening:
    - Unit test stubs for all USE_LOCAL routing paths (Plan 01)
    - user-dal.ts strict equality fix (== → ===)
    - jest.setup.ts explicit USE_LOCAL=false for AWS mode
    - Table name consistency verified across jest.setup.ts and local-global-setup.js
    - .env.example annotated with table name explanation
    - Static audit confirming no handler exports changed
  </what-built>
  <how-to-verify>
    Review the output from Task 1 above:

    1. lib tests: All USE_LOCAL routing tests in dal.spec, event-dal.spec, user-dal.spec should
       show PASS (or at minimum: no test failures in the USE_LOCAL describe blocks)

    2. system-tests: config.spec and httpClient.spec should show PASS with tests covering both
       USE_LOCAL=true and USE_LOCAL=false paths

    3. Handler audit: Review the grep output. Confirm no handler file has a new or changed
       export signature compared to what you expect. The list should show only pre-existing
       exports — no additions, no removals, no signature changes.

    Optional: If you have Docker running locally, you can also run:
       cd /Users/nikody/src/mytaptrack/back-end/system-tests
       USE_LOCAL=true npm run test:local -- --testPathPattern="devices/apps.graphql"
    to confirm a live integration path, but this is NOT required to approve Phase 1.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all unit tests pass and handler signatures are unchanged.
    Or describe any failures — executor will open gap closure plans to address them.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. All unit tests green (lib + system-tests config/httpClient specs)
2. Handler audit shows zero changes to api/src export signatures
3. Human has typed "approved"
</verification>

<success_criteria>
Phase 1 success criteria from ROADMAP.md:
1. Setting USE_LOCAL=true causes DynamoDB, EventBridge, and Cognito clients to connect to Docker-local endpoints instead of AWS — VERIFIED by unit tests
2. System tests read USE_LOCAL and select local or AWS config without any code changes between runs — VERIFIED by config.spec.ts tests
3. System tests targeting AWS run correctly when USE_LOCAL is unset or false — VERIFIED by jest.setup.ts explicit USE_LOCAL=false and config.spec.ts AWS-mode tests
4. No Lambda handler signatures or resolver function exports are modified — VERIFIED by static grep audit
</success_criteria>

<output>
After human approval, create `.planning/phases/01-config-detection/01-03-SUMMARY.md` using the summary template at @/Users/nikody/.claude/get-shit-done/templates/summary.md

Then update `.planning/STATE.md`:
- Phase: 1 of 4 — COMPLETE
- Plan: 3 of 3 complete
- Status: Ready to plan Phase 2
- Progress: [██░░░░░░░░] 25%
</output>
